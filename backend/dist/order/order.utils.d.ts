export declare function normalizePkh(p: string): string;
export declare function normalizeOutputAmount(amount: Array<{
    unit: string;
    quantity: string | number;
}>): Array<{
    unit: string;
    quantity: string;
}>;
